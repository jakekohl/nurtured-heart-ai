# AI Service Configuration
# Options: "ollama" (local development) or "gemini" (cloud API)
AI_SERVICE=ollama

# Ollama Configuration (when AI_SERVICE=ollama)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b

# Google Gemini Configuration (when AI_SERVICE=gemini)
# Get your API key from https://aistudio.google.com/
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# General AI Settings
TEMPERATURE=0.7

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Configuration
# For local development with frontend running on port 5173
CORS_ORIGINS=http://localhost:5173

# Email Service Configuration (Optional)
# Leave empty to disable email functionality
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
